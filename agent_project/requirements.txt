# ─────────────────────────────────────────────────────────────────────────────
# Local AI Agent System — requirements.txt
# Python 3.10+  |  100% offline after first `ollama pull <model>`
# ─────────────────────────────────────────────────────────────────────────────

# Core framework – [tools] extra pulls in crewai-tools automatically
crewai[tools]>=0.80.0

# Ollama LLM backend (via LangChain, used by crewai's LLM wrapper under the hood)
langchain-ollama>=0.2.0

# Free web search — no API key required
# NOTE: v6+ uses the DDGS context-manager API; older ddg() is removed
duckduckgo-search>=6.0.0

# Environment variable management (.env file support)
python-dotenv>=1.0.0

# ─────────────────────────────────────────────────────────────────────────────
# Transitive deps pinned for stability (optional – remove if you prefer loose)
# ─────────────────────────────────────────────────────────────────────────────
pydantic>=2.6.0          # crewai requires pydantic v2
httpx>=0.27.0            # used by Ollama health-check in main.py
